type UserEncounter {
    start_time: Int,
    end_time: Int,
    participants: [User],
}

extend type Query {
    userEncounters(orderBy: _ @orderBy(columns: ["end_time"])): [UserEncounter] @isAdmin @all(model: "\\App\\Models\\ProcessedData\\UserEncounter")
}
